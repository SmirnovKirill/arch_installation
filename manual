1.1 gdisk /dev/sda
1.2 n +550M ef00
1.3 n +121837M 8300
1.4 w

1.4 mkfs.fat -F32 /dev/sda1
1.5 mkfs.ext4 /dev/sda2

1.6 mount /dev/sda2 /mnt
1.7 mkdir /mnt/efi
1.8 mount /dev/sda1 /mnt/efi

2.1 ip link, найти wifi
2.2 ip link set wlp2s0 (заменить на нужное) up
2.3 iw dev wlp2s0 (заменить на нужное) scan	
2.4 wpa_passphrase MYSSID passphrase > /etc/wpa_supplicant/example.conf
2.5 wpa_supplicant -B -i wlp2s0 -c /etc/wpa_supplicant/example.conf
2.6 dhcpcd wlp2s0 (заменить на нужное)

3 pacstrap /mnt base

4 genfstab -U /mnt >> /mnt/etc/fstab

5 arch-chroot /mnt

6.1 ln -sf /usr/share/zoneinfo/Europe/Moscow /etc/localtime
6.2 hwclock --systohc
6.3 Раскомментировать в /etc/locale.gen en_US.UTF-8 UTF-8
6.4 locale-gen
6.5 в /etc/locale.conf записать LANG=en_US.UTF-8

7. passwd

8. pacman -S wpa_supplicant grub efibootmgr vim

9.1 grub-install --target=x86_64-efi --efi-directory=/efi --bootloader-id=GRUB
9.2 grub-mkconfig -o /boot/grub/grub.cfg

Вытащить диск/флешку.

1. systemctl enable dhcpcd.service чтобы заработал Интернет

2. useradd -m kirill

3. passwd kirill

4. pacman -S sudo

5. visudo и дописать рядом с рутом kirill ALL=(ALL) ALL (ну или по аналогии)

6. sudo pacman -S virtualbox-guest-utils если установка на виртуалке, выбрать там опцию с virtualbox-guest-modules-arch

7. sudo pacman -S xorg-server xorg-xinit

7.1 cp /etc/X11/xinit/xinitrc ~/.xinitrc

7.2 cp /etc/X11/xinit/xserverrc ~/.xserverrc

8. sudo pacman -S openbox lxqt-panel xterm pcmanfm-qt xscreensaver

9. sudo pacman -S vim

10. в ~/.bashrc прописать alias vi="vim"

11. заменить в ~/.xinitrc весь блок начинающийся с twm и до конца на exec openbox-session

12. mkdir -p ~/.config

13. cp /etc/xdg/openbox ~/.config -r, поменять в rc.xml число десктопов с 4 на 1

14. cp /etc/xdg/lxqt ~/.config/ -r

15. Поменять содержимое ~/.config/openbox/autostart на
lxqt-panel &
xscreensaver -no-splash &

16. в ~/.xscreensaver поменять timeout на 99:59:59

17. sudo pacman -S alsa-utils, открыть нужные каналы путем нажатия n
