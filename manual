1.1 gdisk /dev/sda
1.2 n +550M ef00
1.3 n +128M 0c01
1.4 n +24576M 8200
1.5 n +65536M 0700
1.6 n 8300
1.7 w

1.8 mkfs.fat -F32 /dev/sda1
1.9 mkswap /dev/sda3
1.10 swapon /dev/sda3
1.11 mkfs.ext4 /dev/sda5

1.12 mount /dev/sda5 /mnt
1.13 mkdir /mnt/efi
1.14 mount /dev/sda1 /mnt/efi

2.1 ip link, найти wifi
2.2 ip link set wlp2s0 (заменить на нужное) up
2.3 iw dev wlp2s0 (заменить на нужное) scan	
2.4 wpa_passphrase MYSSID passphrase > /etc/wpa_supplicant/example.conf
2.5 wpa_supplicant -B -i wlp2s0 -c /etc/wpa_supplicant/example.conf
2.6 dhcpcd wlp2s0 (заменить на нужное)

3 pacstrap /mnt base

4 genfstab -U /mnt >> /mnt/etc/fstab

5 arch-chroot /mnt

6.1 timedatectl set-ntp true
6.2 ln -sf /usr/share/zoneinfo/Europe/Moscow /etc/localtime
6.3 hwclock --systohc
6.4 Раскомментировать в /etc/locale.gen en_US.UTF-8 UTF-8
6.5 locale-gen
6.6 в /etc/locale.conf записать LANG=en_US.UTF-8

7. passwd

8. pacman -S wpa_supplicant grub efibootmgr vim git sudo

9.1 useradd -m kirill
9.2 passwd kirill
9.3 visudo и дописать рядом с рутом kirill ALL=(ALL) ALL (ну или по аналогии)
9.4 su kirill
9.5 cd ~

10 git clone https://github.com/SmirnovKirill/arch_installation.git

11.1 grub-install --target=x86_64-efi --efi-directory=/efi --bootloader-id=GRUB

Если установлена винда, то
11.2 chmod +x add_windows_to_grub.sh
11.3 sudo ./add_windows_to_grub.sh

11.4 grub-mkconfig -o /boot/grub/grub.cfg

12 passwd -l root

13 в /etc/environment дописать
EDITOR=vim
SUDO_EDITOR=vim

14 раскомментировать TotalDownload в /etc/pacman.conf

15.1 pacman -S xorg-server xorg-xrandr openbox lxde-common lxsession lxpanel lxdm lxterminal pcmanfm ttf-dejavu zip unzip imagemagick gpicview
lxde-common и lxsession потому что минимальные требования lxde,
xorg-xrandr чтобы управлять разрешением экрана
15.2 systemctl enable lxdm
15.3 положить lxdm.conf в /etc/lxdm/lxdm.conf

Вытащить диск/флешку.

1 ~/.config/openbox/lxde-rc.xml
1.1 в desktops->number поменять с 2 на 1
1.2 где <context name="Titlebar"> дописать
      <mousebind button="Left" action="Drag">
        <action name="if">
          <maximized>yes</maximized>
            <then>	
              <action name="Unmaximize"/>
              <action name="MoveResizeTo">
	        <x>center</x>
                <y>0</y>
              </action>
            </then>
	</action>
        <action name="Move"/>
      </mousebind>
      <mousebind button="Left" action="DoubleClick">
        <action name="ToggleMaximizeFull"/>
      </mousebind>


2 ~/.config/lxpanel/LXDE/panels/panel
2.1 стереть Button firefox.desktop, убрать следующий space, iconify, space, pager, space, cpu, launchbar
2.2 добавить плагин xkb
2.3 поставить tray перед звуком и сменой раскладки

3 ~/.config/pcmanfm/LXDE/desktop*
3.1 поменять обои
